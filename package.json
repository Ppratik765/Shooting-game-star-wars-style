{
  "name": "wire-frame-shooting",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wasm:build": "wasm-pack build core-engine --target web --out-dir ../pkg",
    "wasm:dev": "npm run wasm:build && npm run dev"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "rollup": "^4.62.2",
    "vite": "^8.0.12",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.6.0"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "simplex-noise": "^4.0.3",
    "three": "^0.184.0"
  }
}
